import { AlertIcon } from "./alert-icon";
import { AlertRoot } from "./alert-root";
import { AlertTitle } from "./alert-title";

export const Alert = {
    Root: AlertRoot,
    Icon: AlertIcon,
    Title: AlertTitle,
}